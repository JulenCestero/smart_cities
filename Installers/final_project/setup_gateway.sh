# Installing webpage services for captive portal
sudo apt-get install apache2
sudo apt-get install php7.2
# Python
sudo apt-get install python3 # already installed
sudo apt-get install ipython
sudo apt-get install python3-pip
# Motion
# sudo apt-get install motion
# cd ~
# mkdir .motion
# sudo cp /etc/motion/motion.conf ~/.motion/motion.conf # Create the config file for Motion
# Wifi service
sudo apt-get install hostapd
sudo apt-get install dnsmasq # Already installed
# Security
sudo apt-get install freeradius
sudo apt-get install openssl # Already installed    

 
